1) get the JSON file that has the credetials from the GC service account
2) Download travic CLI to encrypt the credentials (do not use and check-in plain text accidentally"
	a) you can use docker image to run the travis CLI instaed of actaully downloading and installing the CLI directly on your machine.